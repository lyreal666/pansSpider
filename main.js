"use strict";

const requests = require('./utils/download');
const {sha1, md5} = require('./utils/encryption');
const pansoso = require('./middlewares/pansoso');

const debug = console.debug;

let deal = async () => {

};


let main = async () => {
    await deal();
};
main().then();